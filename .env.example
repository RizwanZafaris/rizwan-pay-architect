# Copy to .env.local for dev, or set in your deployment environment.
# All client-exposed vars MUST be prefixed VITE_ to be inlined by Vite at build time.

# ─── Site identity ───────────────────────────────────────────────────────────
# Used in canonical URLs, og:url, sitemap.xml, and all absUrl() calls.
# Production canonical domain.
VITE_SITE_URL=https://rzifi.com

# Default Open Graph card image (1200×630). Replace with a real branded card
# once one is hosted on R2, Cloudinary, or your own assets bucket.
VITE_OG_IMAGE_URL=

# ─── Contact form ────────────────────────────────────────────────────────────
# Web3Forms access key (https://web3forms.com — free, no sign-up email).
# When set, the contact form POSTs to Web3Forms and the inbox is your email.
# When unset, the form falls back to opening the visitor's mail client (mailto:).
#
# Drop-in alternatives (adjust CONTACT_ENDPOINT + payload in contact.tsx):
#   - Formspree:   set the form's `action` to your Formspree endpoint
#   - Getform:     same pattern
#   - Cloudflare Worker / custom backend: write a server function
VITE_CONTACT_ACCESS_KEY=

# ─── Google Tag Manager ──────────────────────────────────────────────────────
# Container ID. Production default is GTM-TM5BP98G (compiled into the bundle
# when unset). Set to an EMPTY string in .env.local during local dev to opt
# out — keeps your dev sessions out of production analytics.
# Set to a different ID per environment if you have separate staging/prod
# GTM containers.
#
# To track SPA route changes, configure either of these in GTM:
#   - Built-in "History Change" trigger
#   - Custom Event trigger on event name "spa_pageview"
# The site auto-pushes { event: "spa_pageview", page_path, page_location,
# page_title } to dataLayer on every client-side navigation.
VITE_GTM_ID=GTM-TM5BP98G

# ─── Optional analytics / webmaster tools ────────────────────────────────────
# Preferred architecture:
#   - Put GA4, Google Ads, LinkedIn Insight, Meta Pixel and TikTok Pixel inside
#     GTM so one container owns triggers, consent, preview/debug and publishing.
#   - Use the direct variables below only when the tool needs a site-level meta
#     tag or you intentionally want a small direct script outside GTM.
#
# Direct GA4 fallback for the portfolio conversion events. Production default is
# G-F1NK5FJYJY. Set to an EMPTY string if GA4 is fully owned by a healthy GTM
# Google tag to avoid duplicate page views.
VITE_GA_MEASUREMENT_ID=G-F1NK5FJYJY

# Bing Webmaster Tools verification meta:
#   <meta name="msvalidate.01" content="...">
VITE_BING_SITE_VERIFICATION=

# Microsoft Clarity heatmaps/session recordings. Leave empty until you create
# a Clarity project and are comfortable with session-recording privacy.
VITE_MICROSOFT_CLARITY_ID=

# Plausible privacy-friendly analytics, optional. Leave empty if GA4 via GTM is
# the source of truth.
VITE_PLAUSIBLE_DOMAIN=
VITE_PLAUSIBLE_SRC=https://plausible.io/js/script.js
